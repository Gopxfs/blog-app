<h1>Users</h1>
<% @users = User.all %>
<% @users.each do |user| %>
  <div>
    <img src=<%= user.photo %> alt="User <%= user.id %> picture" width="150" height="150">
    <div>
      <h2><%= user.name %></h2>
      <p>Number of posts: <%= user.posts_counter %></p>
    </div>
  </div>
<% end %>