<%= form_with url: create_comment_path, local: true, scope: :comment do |form| %>
  <%= form.text_area :text, placeholder: 'What do you think about this?' %>
  <%= form.submit 'Add comment'%>
<% end %>